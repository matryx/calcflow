LOAD(NTRIG); loads into MACSYMA a set of simplification rules that are
used to simplify trigonometric function whose arguments are of the form:

                                N*%PI
                            FCN(-----)
                                 1O
where FCN is any of the functions SIN, COS, TAN, CSC, SEC and COT.
The rules take effect automatically.
