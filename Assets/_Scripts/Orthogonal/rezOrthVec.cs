using UnityEngine;
using System.Collections;

[ExecuteInEditMode]
public class rezOrthVec : MonoBehaviour
{
    //vector 1
    public ConstraintGrabbable constraintGrabbable;
    //take in whichever axis to project to 

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //do orthog math and transform result vector 
        //transform.localPosition = constraintGrabbable.lastLocalPos;
        //transform.localPosition = constraintGrabbable.lastLocalPos;
    }
}

